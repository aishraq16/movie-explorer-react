function FilterBar({ sortBy, setSortBy, filterYear, setFilterYear, onReset }) {
    // TODO: Replace these placeholder props with the controls you actually want.
    // TODO: Start with only one or two controls so the grid stays simple.
    // TODO: Keep this component reusable by making it controlled from Home.

    return null
}

export default FilterBar